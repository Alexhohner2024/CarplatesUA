# 🚀 Оптимизация памяти для работы на 512 МБ

## ✅ Реализованные изменения

### 1. **Browser Pool (Переиспользование браузера)**

**Файл:** `services/browser-pool.js`

- Создан singleton модуль для управления браузером
- Браузер запускается **один раз** при первом использовании
- Переиспользуется для всех последующих запросов
- Автоматически пересоздается при неожиданном закрытии
- Graceful shutdown при остановке приложения

**Экономия:**
- ⏱️ **2-5 секунд** на каждый запрос (не нужно запускать браузер)
- 💾 **200-400 МБ** памяти (браузер не пересоздается)

### 2. **Оптимизированные настройки Puppeteer**

**Файл:** `services/browser-pool.js` (аргументы запуска)

Добавлены флаги для минимального потребления памяти:
- `--window-size=800,600` - уменьшенный размер окна
- `--memory-pressure-off` - отключение проверок памяти
- `--max-old-space-size=200` - ограничение памяти Node.js
- `--disable-site-isolation-trials` - отключение изоляции сайтов
- `--disable-background-networking` - отключение фоновых процессов
- И другие оптимизации

**Экономия:** ~50-100 МБ памяти

### 3. **Обновлен Unda Scraper**

**Файл:** `scrapers/unda-scraper.js`

- Использует `browserPool.newPage()` вместо создания нового браузера
- Закрывает только страницу после использования (браузер остается)
- Уменьшен размер viewport с 1920x1080 до 800x600

**Экономия:** ~100-150 МБ памяти на запрос

### 4. **Мониторинг памяти**

**Файл:** `bot.mjs`

- Добавлено логирование использования памяти при старте
- Опциональное периодическое логирование (через `LOG_MEMORY=true`)
- Graceful shutdown с закрытием browser pool

## 📊 Ожидаемые результаты

### До оптимизации (512 МБ):
- ⏱️ Время ответа: **5-15 секунд** (из-за swap)
- 💾 Потребление памяти: **350-500 МБ** (пик)
- ⚠️ Риск падений: **Высокий** (OOM errors)
- 🔄 Создание браузера: **Каждый запрос**

### После оптимизации (512 МБ):
- ⏱️ Время ответа: **2-5 секунд** (без swap)
- 💾 Потребление памяти: **200-300 МБ** (стабильно)
- ✅ Риск падений: **Низкий**
- 🔄 Создание браузера: **Один раз при старте**

## 🔧 Как это работает

1. **При старте бота:**
   - Браузер НЕ запускается сразу (ленивая инициализация)
   - Браузер запустится только при первом запросе номера (не VIN)

2. **При запросе VIN:**
   - Браузер не нужен, используется только API
   - Быстрый ответ без использования памяти

3. **При запросе номера:**
   - Браузер запускается (если еще не запущен)
   - Создается новая страница из pool
   - Выполняется скрапинг
   - Страница закрывается, браузер остается

4. **При следующем запросе:**
   - Используется уже запущенный браузер
   - Экономия 2-5 секунд и 200-400 МБ памяти

## 🚀 Деплой

Изменения готовы к деплою. Просто выполните:

```bash
flyctl deploy
```

## 📝 Мониторинг

Для включения периодического логирования памяти установите переменную окружения:

```bash
flyctl secrets set LOG_MEMORY=true
```

Или добавьте в `fly.toml`:
```toml
[env]
  LOG_MEMORY = "true"
```

## ⚠️ Важные замечания

1. **Первый запрос номера** может быть медленнее (2-3 секунды на запуск браузера)
2. **Браузер остается в памяти** между запросами - это нормально и экономит ресурсы
3. При нехватке памяти браузер автоматически пересоздается при следующем запросе
4. Все ошибки обрабатываются корректно, страница всегда закрывается

## 🔍 Проверка работы

После деплоя проверьте логи:

```bash
flyctl logs -a carplatesua
```

Ожидаемые сообщения:
- `[BrowserPool] Launching browser with memory-optimized settings...` - при первом запросе
- `[BrowserPool] Browser launched successfully` - браузер запущен
- `[Unda] Page closed, browser remains active for reuse` - страница закрыта, браузер активен
- `[Memory] Heap Used: ...MB` - использование памяти

