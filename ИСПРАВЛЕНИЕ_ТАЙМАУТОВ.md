# 🔧 Исправление ошибок таймаутов протокола

## 🐌 Проблема
Ошибка `Network.enable timed out` при запросах, что приводило к:
- ⏱️ Ожидание 3 минуты
- ❌ Ошибка "Информация не найдена"
- 🔴 Браузер не может установить соединение с DevTools Protocol

## ✅ Реализованные исправления

### 1. **Увеличен protocolTimeout**

**Файл:** `services/browser-pool.js`

Добавлен `protocolTimeout: 120000` (2 минуты) в настройки запуска браузера:

```javascript
const browser = await puppeteer.launch({
  protocolTimeout: 120000, // 2 минуты для протокола DevTools
  // ...
});
```

**Почему это важно:**
- По умолчанию Puppeteer использует таймаут 180 секунд
- При нехватке памяти или медленной системе это может быть недостаточно
- Увеличенный таймаут дает больше времени на установку соединения

### 2. **Улучшена обработка ошибок при создании страницы**

**Файл:** `services/browser-pool.js`

Добавлена обработка ошибок в методе `newPage()`:

```javascript
async newPage() {
  try {
    const browser = await this.getBrowser();
    const page = await browser.newPage();
    
    // Устанавливаем таймауты для страницы
    page.setDefaultNavigationTimeout(20000); // 20 секунд
    page.setDefaultTimeout(15000); // 15 секунд
    
    return page;
  } catch (error) {
    // Если ошибка связана с протоколом, пересоздаем браузер
    if (error.message && error.message.includes('timed out')) {
      console.error('[BrowserPool] Protocol timeout, recreating browser...');
      this.browser = null;
      const browser = await this.getBrowser();
      // ...
    }
  }
}
```

### 3. **Добавлена retry логика в скрапере**

**Файл:** `scrapers/unda-scraper.js`

- Функция `scrapeUndaVin` теперь принимает параметр `retryCount`
- Максимум 2 повторные попытки (всего 3 попытки)
- При ошибках протокола браузер пересоздается автоматически

```javascript
export async function scrapeUndaVin(plateNumber, retryCount = 0) {
  const maxRetries = 2;
  
  // При ошибке протокола:
  if (error.message.includes('timed out') && retryCount < maxRetries) {
    await browserPool.forceRecreate();
    return await scrapeUndaVin(plateNumber, retryCount + 1);
  }
}
```

### 4. **Добавлен метод forceRecreate()**

**Файл:** `services/browser-pool.js`

Новый метод для принудительного пересоздания браузера:

```javascript
async forceRecreate() {
  console.log('[BrowserPool] Force recreating browser...');
  await this.close();
  await new Promise(resolve => setTimeout(resolve, 1000));
  return await this.getBrowser();
}
```

### 5. **Улучшены сообщения об ошибках**

Теперь пользователь получает более понятные сообщения:
- При таймауте: "Превышено время ожидания ответа от сервера"
- При других ошибках: "Произошла ошибка при получении данных"

## 🔄 Логика работы при ошибках

1. **Первая попытка:**
   - Создается страница из browser pool
   - Выполняется скрапинг
   - При ошибке протокола → переход к шагу 2

2. **Вторая попытка (retry 1):**
   - Браузер принудительно пересоздается
   - Ожидание 2 секунды
   - Повторная попытка скрапинга
   - При ошибке → переход к шагу 3

3. **Третья попытка (retry 2):**
   - Браузер снова пересоздается
   - Ожидание 2 секунды
   - Последняя попытка скрапинга
   - При ошибке → возврат ошибки пользователю

## 📊 Ожидаемые результаты

### До исправления:
- ❌ Ошибка `Network.enable timed out`
- ⏱️ Ожидание 3 минуты
- 🔴 Браузер не пересоздается при ошибках

### После исправления:
- ✅ Автоматическое пересоздание браузера при ошибках
- ⏱️ До 3 попыток с пересозданием браузера
- 🔄 Более надежная работа при проблемах с протоколом
- 📝 Понятные сообщения об ошибках

## ⚠️ Важные замечания

1. **Пересоздание браузера** занимает 2-5 секунд, но это лучше чем полный провал
2. **Retry логика** может увеличить время ответа при ошибках, но повышает надежность
3. **protocolTimeout** увеличен до 2 минут - это должно быть достаточно для большинства случаев
4. При нехватке памяти браузер может падать - retry логика поможет восстановиться

## 🚀 Деплой

Изменения готовы к деплою:

```bash
flyctl deploy
```

## 🔍 Мониторинг

После деплоя проверьте логи:

```bash
flyctl logs -a carplatesua | grep -E "(Unda|BrowserPool|timeout|retry)"
```

Ожидаемые сообщения:
- `[Unda] Starting scrape for plate: ... (attempt 1/3)`
- `[BrowserPool] Protocol timeout, recreating browser...` (при ошибках)
- `[Unda] Protocol error detected, forcing browser recreation and retrying...`
- `[Unda] Starting scrape for plate: ... (attempt 2/3)` (при retry)

## 🎯 Дополнительные рекомендации

Если проблемы продолжаются:

1. **Проверьте использование памяти:**
   ```bash
   flyctl logs -a carplatesua | grep Memory
   ```

2. **Увеличьте protocolTimeout** до 180000 (3 минуты) если нужно

3. **Рассмотрите увеличение retry** до 3 попыток (maxRetries = 3)

4. **Проверьте доступность unda.com.ua** - возможно, сайт недоступен или очень медленный

